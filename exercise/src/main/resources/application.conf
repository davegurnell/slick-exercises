// Use `Database.forConfig("database.employees")` in Scala to reference this configuration:
database.docker_employees = {
  url            = "jdbc:mysql://database:3306/employees?useSSL=false"
  driver         = "com.mysql.cj.jdbc.Driver"
  user           = "dbuser"
  password       = "password"
  connectionPool = "disabled"
}

// More configuration options here:
// https://scala-slick.org/doc/3.4.1/config.html
